const numero = 5;

// Se (numero >= 0 && numero <= 5) ocorrer, faça isso {código}
// Se não faça isso {o código}

if (numero >= 0 && numero <= 5) {
    console.log (`Sim, o número ${numero} está entre 0 e 5`);
} else {
    console.log (`O número ${numero} NÃO está entre 0 e 5`)
}